import React, { useState } from 'react'

export default function update_variants() {

    const [quantity, setQauntity] = useState<boolean>(false)

    return (
        <div>update_variants</div>
    )
}
